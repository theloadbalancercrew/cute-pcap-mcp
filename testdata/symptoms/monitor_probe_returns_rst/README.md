Positive fixture: four short TCP probe-shaped flows target the same service every 30 seconds and each receives a responder RST.

Negative fixture: the same cadence includes successful handshakes, so the responder RST ratio stays below the firing threshold.
