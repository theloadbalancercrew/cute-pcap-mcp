interface:
  display_name: "Load Balancer Path Debug"
  short_description: "Trace VIP, SNAT, pool, and reset-side evidence"
  icon_small: "./assets/icon.svg"
  icon_large: "./assets/icon.svg"
  brand_color: "#DC2626"
  default_prompt: "Use $load-balancer-path-debug to follow this connection through the VIP and pool path."
policy:
  allow_implicit_invocation: true
